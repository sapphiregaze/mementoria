{
	"name": "server",
  "private": true,
  "type": "module",
	"scripts": {
		"build": "rimraf dist && tsc",
		"check": "biome check",
		"check-fix": "biome check --fix",
		"dev": "nodemon",
		"format": "biome format",
		"lint": "biome lint",
		"serve": "node dist/server.js",
		"start": "pnpm serve",
		"test": "echo \"No test runner configured. Add one like Jest or Vitest to run tests.\""
	},
	"dependencies": {
		"fastify": "^5.4.0"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"nodemon": "^3.1.10",
		"rimraf": "^6.0.1",
		"ts-node": "^10.9.2",
		"typescript": "^5.8.3"
	}
}
